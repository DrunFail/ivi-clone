import Modal from './Modal';

export default {
    title: 'ui/Modal',
    component: Modal,
};

export const Default = () => (
    <Modal callback={() => {}} visible={true}>
        sda
    </Modal>
);
